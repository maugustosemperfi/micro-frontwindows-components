import React from "react";
import MuiCardFooter, { CardActionsProps } from "@mui/material/CardActions";

const CardFooter = (props: CardActionsProps) => <MuiCardFooter {...props} />;

export default CardFooter;
